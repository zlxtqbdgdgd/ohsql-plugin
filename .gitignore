node_modules/
*.log
.DS_Store
dist/
.tmp/
.npmrc

# sqlite WAL / SHM ephemeral files (the .sqlite itself IS committed)
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal

# 历次审计的 sqlite 备份
*.sqlite.bak-*

# lint-kb-quotes 用的 URL 文本缓存 (CI 跑 refresh-url-cache 重建,不入仓)
plugins/perf-kp-sql/tools/url-cache/

# rules.json 备份 (audit 修复时手动产生 · 不入仓)
*.json.bak-*

# @xenova/transformers 自动下载的 ONNX 模型 (运行时按需 cache · 不入仓)
plugins/perf-kp-sql/data/models/

# 一次性 audit 的临时 python 脚本
plugins/perf-kp-sql/_audit_counts.py
plugins/perf-kp-sql/tools/audit_kb_urls.py

# perf-kp-sql sqlite WAL/SHM (主体 knowledge.sqlite IS committed · 随 plugin install 发布)
# (顶部 *.sqlite-wal / *.sqlite-shm 通用规则已覆盖 · 这里不再重复)
