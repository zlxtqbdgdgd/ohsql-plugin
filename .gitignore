node_modules/
*.log
.DS_Store
dist/
.tmp/
.npmrc

# sqlite WAL / SHM ephemeral files (the .sqlite itself IS committed)
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal

# MiniLM model cache (downloaded by kb-build at runtime · not versioned)
plugins/*/data/models/
