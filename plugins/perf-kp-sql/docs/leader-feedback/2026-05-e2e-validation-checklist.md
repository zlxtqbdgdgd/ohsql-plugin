# 五一节内成果端到端验证 checklist

> 用途:用户在鲲鹏测试机(124.70.180.36)上真跑 `/perf-kp-sql` · 按 checklist 验证 Day 1-3 改动是否都生效。
>
> 时间预估:跑 1 次完整诊断 ≈ 5-10 分钟 · 复盘 checklist ≈ 5 分钟。

---

## 准备

测试机 SSH:
- host: `124.70.180.36`
- user: `root`
- port: 22
- 凭据(已给):用户保管

诊断场景(至少跑 1 次 · 推荐 3 种):
1. **CPU 高场景**(原来用过 · 现成测试场景)
2. **慢查询场景**(可选 · 构造一个)
3. **WT cache 压力场景**(可选 · 构造一个)

---

## checklist · 跑完 perf-kp-sql 后逐项核对

### 1. 触发后开场白(对应领导反馈 1.1)

跑 `/perf-kp-sql host=124.70.180.36 user=root password=...` 后 · 在收 SSH 凭据 / 收问题现象之前 · chat 应该立刻打:

```
[perf-kp-sql · 鲲鹏 + MongoDB 性能诊断]

我是一个鲲鹏场景下泛数据库性能诊断 skill,基于 202 条诊断案例 + NotebookLM 联网知识库,会通过以下流程定位性能瓶颈与根因:

  1. 环境信息采集
  2. 诊断案例匹配
  3. 诊断指标采集
  4. 多源综合诊断
  5. 报告生成

中途会问你:SSH 凭据、问题现象。
```

- [ ] **chat 里看到这段开场白**(对照原文 · 看 5 步流程 + "中途会问你 ..." 是否齐全)
- [ ] 报告 .md 文件头部也有这段(等下打开 md 看)

### 2. 术语统一(对应领导反馈 1.2)

跑流程时观察 chat 输出 + 看 md 报告:

- [ ] 提示语用 **凭据**(不混用密码 / 账号 / 鉴权 · 但 NLM 上下文用"鉴权"是合理的)
- [ ] 提示语用 **问题现象**(不混用问题描述 / 现象描述 / 症状)
- [ ] 提示语用 **采集**(不混用收集 / 抓 / 拉取)
- [ ] 提示语用 **诊断**(不混用分析 / 排查)

### 3. Phase 3 task 子项(对应领导反馈 1.3)

进入 Phase 3 诊断指标采集时 · task list 应显示:

```
✔ 1. 环境信息采集
✔ 2. 诊断案例匹配
◼ 3. 诊断指标采集
    ✔ OS:CPU / 内存 / 磁盘 / 网络
    ⏳ MongoDB:连接池 / 慢查询 / 锁竞争 / 存储引擎
◻ 4. 多源综合诊断
◻ 5. 报告生成
```

- [ ] **task list 显示 5 步主结构**(不是 6 phase 的旧结构)
- [ ] **Phase 3 显示 OS / MongoDB 两个子项**(不是只一行"诊断指标采集")
- [ ] 子项内容是 `CPU / 内存 / 磁盘 / 网络` + `连接池 / 慢查询 / 锁竞争 / 存储引擎`

### 4. 来源标注(对应领导反馈 1.4)

打开 md 报告 · 看"诊断结果"主表:

- [ ] 每个原子事实都带 `[CASE]` / `[NLM]` / `[OBS]` / `[LLM]` / `[IDX]` 5 标签之一
- [ ] chat 输出里这些标签**不出现**(被 stripChatTags 剥掉 · 干净)
- [ ] md 报告里这些标签**保留**(给审计用)

### 5. 归档单目录(对应领导反馈 1.5)

跑完诊断后 · 检查归档:

```bash
ls ~/.perf-kp-sql/runs/
```

应该看到一个新建的 `<TS>/` 目录(类似 `20260505-103015/`):

- [ ] 目录存在
- [ ] 目录里有 `report.md`(诊断报告)
- [ ] 目录里有 `env.txt`(环境信息)
- [ ] 目录里有 `collect-os.txt`(OS 层采集)
- [ ] 目录里有 `collect-mongo.txt`(数据库层采集)
- [ ] 如果触发了火焰图 · 还有 `flame.svg`(同目录)
- [ ] **不应**有散落到 `~/.perf-kp-sql/{tmp,reports,flame,experiments}/` 的新文件

### 6. 主要问题段(对应领导反馈 1.1 中的"诊断结论")

> ⚠️ **此项验证当前不计入通过条件**:`llm-boundaries.md` 5.3 介入点(0.48.0 起新增)已显式标"缺兜底" · 节后改进路线已列(模板填空 / LLM 自检 / format-chat lint 交叉引用 三选一)。当前 LLM 可能写"主要问题"段也可能省略 · 这项观察用 · 不算 ⚠️ 部分。

打开 md 报告 · 看 metadata card 之后:

- [ ] 有一段 `## 主要问题`(或类似命名)· 给一句话结论
- [ ] 结论有 `严重 N · 警告 N · 提示 N · 正常 N` 计数
- [ ] 结论有"建议先改 X · 影响最大"这种引导

(注:如果 LLM 没生成这段 · 说明 SKILL.md 的开场白规则没让 LLM 写主要问题段 · 这是 1.1 实施的潜在缺陷 · 需要补)

### 7. 报告引用(质量验证)

打开 md 报告 · 看 `## 参考资料汇总` 段:

- [ ] 引用的 URL 都是真实的(可点开)
- [ ] URL 来自案例库已有的 source_url(可以从 `data/source-cache/manifest.json` 查) · 不是 LLM 编的
- [ ] 至少有 2 个引用来源(单源容易偏)

### 8. 火焰图(若 Phase 3.A.3 触发)

跑了火焰图采集的话:

- [ ] `~/.perf-kp-sql/runs/<TS>/flame.svg` 存在(scp 拉到本地了 · 不是只在远端)
- [ ] 报告 `## 火焰图分析` 段有 Top-N 函数表 + 文字解读

---

## 验证后该做

- 把验证结果(逐项 ✓ / ✗)告诉我
- 如果某项 ✗ · 我会基于 SKILL.md / format-chat.mjs 的实际逻辑分析为什么没生效 · 并补修
- 全部 ✓ → Day 4 端到端验证通过 → 可以给领导报告了

---

## 失败兜底

如果跑诊断中途出错(NLM 鉴权过期 / SSH 连不上 / 等):
- 我已经在 SKILL.md Phase 0.10 加了 NLM 鉴权失败恢复流程
- 如果碰到 · chat 里会引导你重登录浏览器
- 跑通后再继续跑诊断
