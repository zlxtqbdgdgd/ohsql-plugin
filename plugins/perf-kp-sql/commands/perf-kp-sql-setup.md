---
description: Verify perf-kp-sql runtime + register optional NotebookLM CLI for refresh
allowed-tools: Bash, Read, Skill, AskUserQuestion
---

Invoke the `perf-kp-sql-setup` skill to walk the user through environment verification and optional NotebookLM CLI registration.

Use the `Skill` tool with skill name `perf-kp-sql-setup` (no plugin namespace prefix needed when invoked from this command).

The skill itself drives the flow, do not pre-empt it.
